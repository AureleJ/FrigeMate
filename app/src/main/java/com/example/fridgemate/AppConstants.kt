package com.example.fridgemate

import androidx.compose.ui.graphics.Color

// --- Définition des Couleurs Globales (Déplacées ici) ---
val GreenPrimary = Color(0xFF4CAF50) // Notre vert FridgeMate
val GreenLight = Color(0xFFE8F5E9)  // Vert très clair pour les fonds/indicateurs
val TextDark = Color(0xFF2C3E50)    // Texte principal sombre
val TextGray = Color(0xFF7F8C8D)    // Texte secondaire/gris
val BgColor = Color(0xFFF5F7F6)     // Fond de l'application

// --- DÉFINITIONS DES COULEURS (À mettre en haut du fichier) ---
val WebBg = Color(0xFFF7F8FC)          // Fond gris très clair
val WebCardBg = Color.White            // Fond des cartes
val WebTextDark = Color(0xFF1A202C)    // Noir doux (Celui qu'il te manque)
val WebTextGray = Color(0xFF718096)    // Gris pour les textes secondaires
val WebRed = Color(0xFFE53E3E)         // Rouge pour les alertes
val WebGreen = Color(0xFF48BB78)       // Vert du site